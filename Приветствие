#include <iostream>
#include <string>
using namespace std;

int main()
{
    string name;
    cout << "Введите ваше имя: ";
    getline(cin, name);
    cout << "Привет, " << name << "!" << endl;
    cout << "Привет, " << name << "!" << endl;
    return 0;
}
